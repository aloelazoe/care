{
  "name": "care",
  "version": "0.2.0",
  "description": "cozy self-care reminders ✨💜🌈",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "build-mac": "electron-packager . --platform=darwin --arch=x64 --out=build/ --overwrite --icon=icons/circle-icons-heart.icns && echo \"built for mac\""
  },
  "author": "Elkie Nova (@aloelazoe)",
  "license": "ISC",
  "dependencies": {
    "fs-extra": "^8.1.0"
  },
  "devDependencies": {
    "electron": "^6.0.4",
    "electron-packager": "^14.0.5"
  }
}
